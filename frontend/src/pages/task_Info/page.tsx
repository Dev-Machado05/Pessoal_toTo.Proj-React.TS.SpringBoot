import NavBar from "../../assets/components/global/navbar";

const taskInfo = () => { 
    return (
        <main className="taskInfoContainer">
            <NavBar s_page="view"/>
        </main>
    )
}

export default taskInfo;